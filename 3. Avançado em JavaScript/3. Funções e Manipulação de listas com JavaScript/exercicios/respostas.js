//--------------------------------------------------------------------------
function saudacao(nome){
    console.log(`Olá, ${nome}`); 
}
//--------------------------------------------------------------------------
function operar(x , y, operacao){
    return operacao(x, y); 
}
//--------------------------------------------------------------------------
const dobro = (n) => n * 2; 
//--------------------------------------------------------------------------

//--------------------------------------------------------------------------
const Objeto1 = {
    nome: "Daniel"
}
const Objeto2 = {
    nome: "Gabrielle"
}
//--------------------------------------------------------------------------
function saudacao_obj(){
    return this.nome; 
}
console.log(saudacao_obj.call(Objeto1));
console.log(saudacao_obj.call(Objeto2));
//--------------------------------------------------------------------------
const list = [];
for(let i = 1; i <= 10; i++){
    list[i -1] = i; 
}
list.forEach((value) => {
    console.log(value); 
})
//--------------------------------------------------------------------------
const list_pares = list.filter((value) => value % 2 === 0); 
//--------------------------------------------------------------------------
const list_mult_3 = list.map((value) => value * 3); 
//--------------------------------------------------------------------------
console.log(list.reduce((value, value2) => value + value2), 0); 
//--------------------------------------------------------------------------
const list_combinada = list.filter((value) => value > 5).map((value) => value * 2).reduce(((acc, num) => acc + num), 0); 
//--------------------------------------------------------------------------